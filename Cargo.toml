[package]
name = "tosmole"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = true
strip = true
opt-level = 3

[dependencies]
flate2 = { version = "1.1.2", default-features = false, features = ["zlib"] }
quick-xml = "0.38.3"
